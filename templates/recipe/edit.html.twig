{% extends 'base.html.twig' %}

{% block title %}Edit Recipe{% endblock %}

{% block body %}
    <div class="container">
        <div class="jumbotron">
        <h1>Edit Recipe</h1>

        {{ include('recipe/_form.html.twig', {'button_label': 'Update'}) }}
<div class="row">
        <div class="col-lg-6">
    <a class="btn btn-info" href="{{ path('recipe_index') }}">Retour à la liste</a>
</div>
   <div class="col-lg-6">
       {{ include('recipe/_delete_form.html.twig') }}
   </div>
</div>
    </div>
    </div>
{% endblock %}
{% block javascripts %}
    <script src="{{ asset('js/recipe_content_directions.js') }}"></script>
    <script src="{{ asset('js/recipe_content_ingredients.js') }}"></script>
{% endblock %}